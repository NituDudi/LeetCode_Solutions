# Maximal Rectangle

## 💡 Explanation
Convert each row into a histogram of heights.  
For every row, compute the largest rectangle area using the **Largest Rectangle in Histogram** method with a stack.  
Update the maximum area found across all rows.